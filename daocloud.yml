image: go1.4

#services:
#  - mongodb

# using default docker-link env
#env:
#  - MONGODB_PORT_27017_TCP_ADDR=mongodb
#  - MONGODB_PORT_27017_TCP_PORT=27017

script:
  - export GOPATH=$PWD
  - export PATH=$PATH:$GOPATH/bin
  - sudo apt-get update
  - curl http://hanbowang-test01.daoapp.io/

notify:
  email:
    recipients:
      - dev@smartekworks.com
    on_success: on
    on_failure: off
